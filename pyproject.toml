[project]
name = "churn-prediction"
version = "0.1.0"
description = "Churn prediction project for MLflow course"
readme = "README.md"
requires-python = "==3.10.0"
dependencies = [
    "mlflow==3.11.1",
    "scikit-learn",
    "pandas",
    "numpy",
    "matplotlib",
    "seaborn",
    "psycopg2-binary",
    "gunicorn",
]
